//for loopkar raha han



const table = (table) => {
    for (let i=0 ; i<=10 ; i++){
        let result=0;
        result=table* i
        console.log(table + `x${i}=` + result);
    }
    
}
table(8);